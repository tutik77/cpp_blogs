# Blog Platform Frontend

Простой и красивый фронтенд для блог-платформы на C++/Drogon.

## 🚀 Быстрый старт

### Предварительные требования

Убедитесь, что запущены оба микросервиса:
- **Auth Service** на порту `3000`
- **App Service** на порту `3001`

### Запуск фронтенда

Просто откройте `index.html` в браузере:

```bash
# Из папки frontend
start index.html
```

Или используйте любой простой HTTP-сервер:

```bash
# Python
python -m http.server 8080

# Node.js (если установлен http-server)
npx http-server -p 8080
```

Затем откройте в браузере: `http://localhost:8080`

## 🎨 Возможности

- ✨ Современный темный дизайн
- 🔐 Регистрация и авторизация
- 📝 Создание постов
- ❤️ Лайки постов
- 💬 Комментарии
- 🎭 Плавные анимации
- 📱 Адаптивный дизайн

## 🔧 API Endpoints

### Auth Service (порт 3000)
- `POST /v1/Auth/reg` - регистрация
- `POST /v1/Auth/login` - вход

### App Service (порт 3001)
- `GET /feed` - получить ленту постов
- `POST /posts` - создать пост
- `GET /posts/{id}` - получить пост
- `DELETE /posts/{id}` - удалить пост
- `POST /posts/{id}/like` - лайкнуть пост
- `DELETE /posts/{id}/like` - убрать лайк
- `GET /posts/{id}/comments` - получить комментарии
- `POST /posts/{id}/comments` - добавить комментарий

## 📁 Структура

```
frontend/
├── index.html      # Главная страница
├── styles.css      # Стили
├── app.js          # Логика приложения
└── README.md       # Этот файл
```

## 🎯 Использование

1. **Регистрация**: Нажмите "Регистрация" и создайте аккаунт
2. **Вход**: Войдите с вашими учетными данными
3. **Создание поста**: Нажмите "Создать пост" и напишите что-нибудь интересное
4. **Взаимодействие**: Лайкайте посты и оставляйте комментарии

## 🛠 Технологии

- Vanilla JavaScript (ES6+)
- Vanilla CSS (с CSS переменными)
- HTML5
- Fetch API для работы с REST API
- LocalStorage для хранения токена

## 📝 Примечания

- Токен авторизации хранится в LocalStorage
- CORS должен быть настроен на бэкенде для работы с localhost
- Все запросы к API используют JSON формат

## 🐛 Troubleshooting

### CORS ошибки
Убедитесь, что серверы настроены на разрешение CORS запросов с `http://localhost`.

### Не загружается лента
Проверьте, что:
1. Auth Service запущен на порту 3000
2. App Service запущен на порту 3001
3. Вы успешно авторизовались
4. В консоли браузера нет ошибок

### Проблемы с авторизацией
- Проверьте формат ответа от `/v1/Auth/login` - должен содержать поле `token`
- Убедитесь, что пользователь существует в базе данных
